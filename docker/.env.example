# Zona horaria del servidor (usada por el nodo de horarios del IVR)
TZ=Europe/Madrid

# Dominio público (solo se usa en producción con docker-compose.prod.yml).
# Déjalo vacío en desarrollo local.
DOMAIN=

# PostgreSQL
POSTGRES_USER=ireparo
POSTGRES_PASSWORD=ireparo
POSTGRES_DB=ireparo
